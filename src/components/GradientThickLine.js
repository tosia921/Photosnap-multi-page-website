import React from 'react'
import styled from 'styled-components'

const GradientThickLine = () => <StyledGradientLine />

export default GradientThickLine

const StyledGradientLine = styled.div`
    width: 12.8rem;
    height: 0.6rem;
    background: var(--MainAccentGradient);
`
